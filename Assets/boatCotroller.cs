
using UnityEngine;


public class boatCotroller : MonoBehaviour
{
/*    public GameObject Boat;
    public GameObject Player;
    public GameObject PlayerBackup;
    private bool inBoat = false;
    Boat boatScript;

    private void Start()
    {
        boatScript = GetComponent<Boat>();
        boatScript.enabled = false;

    }
    void OnTriggerStay(Collider other)
    {
        if(other.gameObject.tag == "Player" && inBoat = false && Input.GetKey(KeyCode.E))
        {
            Player.SetActive(false);
            PlayerBackup.setActive(true);
            Player.transform.parent = Boat.transform;
            boatScript.enabled = true;
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if(other.gameObject.tag == "Player")
        {

        }
    }
    void Update()
    {
        if(Boat == true && Input.GetKey(KeyCode.F))
        {
            Player.SetActive(true);
            Player.transform.parent = null;
            PlayerBackup.SetActive(false);
            boatScript.enabled = false;
            inBoat = false;
        }
    }

    */
}
